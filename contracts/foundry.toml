[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.25"

# `EntityRegistry.sol` is interface-only — kept here so SDK consumers
# can `forge build` against a canonical ABI. No bytecode is deployed.
